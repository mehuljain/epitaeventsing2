<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Events for Friday - 27th March 2015{% endblock %}</title>
        {% block stylesheets %}
        <link href="{{ asset('bundles/eventsevents/css/style.css') }}" type="text/css" rel="stylesheet"/>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            <p>Logged in as <b>{{ app.user.username }}</b>({{app.user.email}})<a style="padding-left: 50px;" href="{{path('fos_user_security_logout')}}">Logout</a></p>
              <div id="main">
{% block content %}
    <p>Please register for the events to be held on Friday, 27th March 2015.</p>    
    {% for flashMessage in app.session.flashbag.get('error') %}        
    <div class="flash-error">
        {{ flashMessage }}
    </div>
    {% endfor %}
    <div class="candidatecontent">            
    <div id="form">                         
     <br>
    <form action="{{ path('eventtwo') }}" method="post" {{ form_enctype(form) }} name="eventtwo">
            <table>       
                    {# Event type 1 #}
                    
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype8) }}</b></div></td>
                    <tr>
                    <table>
                        {% for file in form.eventtype8.children %}                            
                            <tr>
                                <td>{{ form_widget(file)}}</td>
                                <td>{{(file.vars.label)}}</td>
                            </tr>                                
                        {% endfor %}
                    </table>
                    </tr>
                    <td><span class="errors">{{ form_errors(form.eventtype8) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    {# Event type 2 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype9) }}</b></div></td>
                        <tr>
                            <table>
                                {% for file in form.eventtype9.children %}                            
                                    <tr>
                                        <td>{{ form_widget(file)}}</td>
                                        <td>{{(file.vars.label)}}</td>
                                    </tr>                                
                                {% endfor %}
                            </table>
                        </tr>
                        <td><span class="errors">{{ form_errors(form.eventtype9) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    {# Event type 3 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype10) }}</b></div></td>
                        <tr>
                            <table>
                                {% for file in form.eventtype10.children %}                            
                                    <tr>
                                        <td>{{ form_widget(file)}}</td>
                                        <td>{{(file.vars.label)}}</td>
                                    </tr>                                
                                {% endfor %}
                            </table>
                        </tr>
                        <td><span class="errors">{{ form_errors(form.eventtype10) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                </table>            
            <table>
                <tbody>
                    <tr>
                        <td style="padding-left: 50px;padding-top: 10px;"><input type="submit" value="Submit" id="submit"></td>
                    </tr>
                </tbody>
            </table>            
{{ form_end(form) }}            
            </div>         
             </div>
            {% endblock content %}   
            </div>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
