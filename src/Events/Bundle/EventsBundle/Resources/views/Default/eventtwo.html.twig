<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Events for Thursday - 24th March 2016{% endblock %}</title>
        {% block stylesheets %}
        <link href="{{ asset('bundles/eventsevents/css/style.css') }}" type="text/css" rel="stylesheet"/>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            <p>Logged in as <b>{{ app.user.username }}</b>({{app.user.email}})<a style="padding-left: 50px;" href="{{path('fos_user_security_logout')}}">Logout</a></p>
              <div id="main">
{% block content %}
    <p>Please register for the events to be held on Thursday, 24th March 2016.</p>    
    {% for flashMessage in app.session.flashbag.get('error') %}        
    <div class="flash-error">
        {{ flashMessage }}
    </div>
    {% endfor %}
    <div class="candidatecontent">            
    <div id="form">                         
     <br>
    <form action="{{ path('eventtwo') }}" method="post" {{ form_enctype(form) }} name="eventtwo">
            <table>       
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype4) }}</b></div></td>
                    <tr>
                    <table>
                        {% for file in form.eventtype4.children %}                            
                            <tr>
                                <td>{{ form_widget(file)}}</td>
                                <td>{{(file.vars.label)}}</td>
                            </tr>                                
                        {% endfor %}
                    </table>
                    </tr>
                    <td><span class="errors">{{ form_errors(form.eventtype4) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    {# Event type 1 #}
                    
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype5) }}</b></div></td>
                    <tr>
                    <table>
                        {% for file in form.eventtype5.children %}                            
                            <tr>
                                <td>{{ form_widget(file)}}</td>
                                <td>{{(file.vars.label)}}</td>
                            </tr>                                
                        {% endfor %}
                    </table>
                    </tr>
                    <td><span class="errors">{{ form_errors(form.eventtype5) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    {# Event type 2 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype6) }}</b></div></td>
                        <tr>
                            <table>
                                {% for file in form.eventtype6.children %}                            
                                    <tr>
                                        <td>{{ form_widget(file)}}</td>
                                        <td>{{(file.vars.label)}}</td>
                                    </tr>                                
                                {% endfor %}
                            </table>
                        </tr>
                        <td><span class="errors">{{ form_errors(form.eventtype6) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    {# Event type 3 #}
                    <tr>
                        <td><div class="formlabelregister"><b>{{ form_label(form.eventtype7) }}</b></div></td>
                        <tr>
                            <table>
                                {% for file in form.eventtype7.children %}                            
                                    <tr>
                                        <td>{{ form_widget(file)}}</td>
                                        <td>{{(file.vars.label)}}</td>
                                    </tr>                                
                                {% endfor %}
                            </table>
                        </tr>
                        <td><span class="errors">{{ form_errors(form.eventtype7) }}</span></td>
                    </tr>                   
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                    <tr></tr>
                </table>            
            <table>
                <tbody>
                    <tr>
                        <td style="padding-left: 50px;padding-top: 10px;"><input type="submit" value="Submit" id="submit"></td>
                    </tr>
                </tbody>
            </table>            
{{ form_end(form) }}            
            </div>         
             </div>
            {% endblock content %}   
            </div>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
